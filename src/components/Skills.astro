---
interface Skill {
  name: string;
  icon: string;
}

interface Props {
  skills: Record<string, Skill[]>;
}

const { skills } = Astro.props;
---

<section class="space-y-5 animate-section">
  <h2
    class="text-3xl font-bold bg-gradient-to-r from-emerald-600 to-teal-600 dark:from-emerald-400 dark:to-teal-400 bg-clip-text text-transparent"
  >
    Skills
  </h2>
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    {
      Object.entries(skills).map(([category, skillList], index) => (
        <div
          class="skill-card bg-white dark:bg-slate-800/50 backdrop-blur-sm border-2 border-emerald-100 dark:border-emerald-900/30 rounded-2xl p-6 shadow-md hover:shadow-xl hover:border-emerald-300 dark:hover:border-emerald-600 hover:scale-[1.02] transition-all duration-300"
          style={`animation-delay: ${index * 0.1}s`}
        >
          <h3 class="text-lg font-bold bg-gradient-to-r from-emerald-600 to-teal-600 dark:from-emerald-400 dark:to-teal-400 bg-clip-text text-transparent mb-4 pb-2 border-b-2 border-emerald-200 dark:border-emerald-800/30">
            {category}
          </h3>
          <div class="flex flex-wrap gap-2">
            {skillList.map((skill, skillIndex) => (
              <span
                class="skill-badge inline-flex items-center gap-2 px-3 py-2 text-sm font-semibold text-emerald-700 dark:text-emerald-200 bg-gradient-to-br from-emerald-50 to-teal-50 dark:from-emerald-900/20 dark:to-teal-900/20 border border-emerald-200 dark:border-emerald-800/30 rounded-xl hover:border-emerald-400 dark:hover:border-emerald-500 hover:shadow-md hover:scale-105 transition-all duration-200"
                style={`animation-delay: ${index * 0.1 + skillIndex * 0.05}s`}
              >
                <img
                  src={`https://cdn.simpleicons.org/${skill.icon}`}
                  alt={`${skill.name} logo`}
                  class="w-4 h-4"
                />
                {skill.name}
              </span>
            ))}
          </div>
        </div>
      ))
    }
  </div>
</section>
